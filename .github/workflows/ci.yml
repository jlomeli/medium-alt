name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: typecheck + lint + unit + api
    runs-on: ubuntu-latest
    timeout-minutes: 15

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: medium
          POSTGRES_PASSWORD: medium
          POSTGRES_DB: medium_alt
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U medium -d medium_alt"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
      mailpit:
        image: axllent/mailpit:latest
        ports:
          - 1025:1025
          - 8025:8025

    env:
      DATABASE_URL: postgresql://medium:medium@localhost:5432/medium_alt?schema=public
      DIRECT_URL: postgresql://medium:medium@localhost:5432/medium_alt?schema=public
      AUTH_SECRET: ci-test-secret-not-real-do-not-use
      AUTH_URL: http://localhost:3000
      AUTH_TRUST_HOST: "true"
      # 127.0.0.1 not localhost — nodemailer prefers IPv6 which Mailpit
      # doesn't bind to, wasting the SMTP connect timeout on each send.
      SMTP_HOST: 127.0.0.1
      SMTP_PORT: "1025"
      # Enables env-gated dev/test seams under /api/test/* — see
      # docs/specs/auth.md §Testing seams.
      E2E: "1"
      EMAIL_FROM: "Medium-Alt CI <ci@medium-alt.local>"
      E2E_MAILPIT_URL: http://localhost:8025
      NEXT_PUBLIC_APP_URL: http://localhost:3000

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install deps
        run: pnpm install --frozen-lockfile

      - name: Prisma generate + migrate
        run: |
          pnpm prisma generate
          pnpm prisma migrate deploy || pnpm prisma db push

      # Dev-seed drift check. Runs the baseline scenario against the
      # freshly-migrated CI database — a schema/validator change that
      # breaks the seed fails the PR that caused it, instead of biting
      # the next developer to run `pnpm db:reset`. See
      # docs/specs/dev-seed.md § CI wiring.
      - name: Seed (drift check)
        run: pnpm db:seed

      - name: Typecheck
        run: pnpm typecheck

      - name: Lint
        run: pnpm lint

      - name: Unit tests
        run: pnpm test:unit

      - name: Install Playwright browsers
        run: pnpm exec playwright install --with-deps chromium

      - name: API tests
        run: pnpm exec playwright test --project=api

      - name: Build (smoke)
        run: pnpm build
